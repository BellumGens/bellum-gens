import { SideStratsPipe } from './sidestrats.pipe';

describe('TsidestratsPipe', () => {
  it('create an instance', () => {
    const pipe = new SideStratsPipe();
    expect(pipe).toBeTruthy();
  });
});

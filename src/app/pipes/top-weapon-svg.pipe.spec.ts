import { TopWeaponSvgPipe } from './top-weapon-svg.pipe';

describe('TopWeaponSvgPipe', () => {
  it('create an instance', () => {
    const pipe = new TopWeaponSvgPipe();
    expect(pipe).toBeTruthy();
  });
});

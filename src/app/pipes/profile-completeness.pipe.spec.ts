import { ProfileCompletenessPipe } from './profile-completeness.pipe';

describe('ProfileCompletenessPipe', () => {
  it('create an instance', () => {
    const pipe = new ProfileCompletenessPipe();
    expect(pipe).toBeTruthy();
  });
});

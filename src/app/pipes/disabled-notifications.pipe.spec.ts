import { DisabledNotificationsPipe } from './disabled-notifications.pipe';

describe('DisabledNotificationsPipe', () => {
  it('create an instance', () => {
    const pipe = new DisabledNotificationsPipe();
    expect(pipe).toBeTruthy();
  });
});

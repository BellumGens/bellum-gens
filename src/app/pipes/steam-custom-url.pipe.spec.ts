import { SteamCustomUrlPipe } from './steam-custom-url.pipe';

describe('SteamCustomUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new SteamCustomUrlPipe();
    expect(pipe).toBeTruthy();
  });
});

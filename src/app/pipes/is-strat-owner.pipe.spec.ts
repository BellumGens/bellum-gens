import { IsStratOwnerPipe } from './is-strat-owner.pipe';

describe('IsStratOwnerPipe', () => {
  it('create an instance', () => {
    const pipe = new IsStratOwnerPipe();
    expect(pipe).toBeTruthy();
  });
});

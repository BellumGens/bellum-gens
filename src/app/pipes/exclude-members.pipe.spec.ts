import { ExcludeMembersPipe } from './exclude-members.pipe';

describe('ExcludeMembersPipe', () => {
  it('create an instance', () => {
    const pipe = new ExcludeMembersPipe();
    expect(pipe).toBeTruthy();
  });
});

import { ProfileMapsCompletePipe } from './profile-maps-complete.pipe';

describe('ProfileMapsCompletePipe', () => {
  it('create an instance', () => {
    const pipe = new ProfileMapsCompletePipe();
    expect(pipe).toBeTruthy();
  });
});

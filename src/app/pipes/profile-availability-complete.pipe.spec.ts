import { ProfileAvailabilityCompletePipe } from './profile-availability-complete.pipe';

describe('ProfileAvailabilityCompletePipe', () => {
  it('create an instance', () => {
    const pipe = new ProfileAvailabilityCompletePipe();
    expect(pipe).toBeTruthy();
  });
});

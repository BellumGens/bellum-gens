import { ActiveDutyMapsPipe } from './active-duty-maps.pipe';

describe('ActiveDutyMapsPipe', () => {
  it('create an instance', () => {
    const pipe = new ActiveDutyMapsPipe();
    expect(pipe).toBeTruthy();
  });
});

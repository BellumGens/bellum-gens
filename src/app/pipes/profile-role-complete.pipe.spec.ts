import { ProfileRoleCompletePipe } from './profile-role-complete.pipe';

describe('ProfileRoleCompletePipe', () => {
  it('create an instance', () => {
    const pipe = new ProfileRoleCompletePipe();
    expect(pipe).toBeTruthy();
  });
});

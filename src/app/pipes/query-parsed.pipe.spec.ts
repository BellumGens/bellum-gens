import { QueryParsedPipe } from './query-parsed.pipe';

describe('QueryParsedPipe', () => {
  it('create an instance', () => {
    const pipe = new QueryParsedPipe();
    expect(pipe).toBeTruthy();
  });
});

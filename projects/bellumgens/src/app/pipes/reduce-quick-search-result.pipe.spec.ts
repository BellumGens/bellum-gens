import { ReduceQuickSearchResultPipe } from './reduce-quick-search-result.pipe';

describe('ReduceQuickSearchResultPipe', () => {
  it('create an instance', () => {
    const pipe = new ReduceQuickSearchResultPipe();
    expect(pipe).toBeTruthy();
  });
});

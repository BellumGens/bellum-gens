import { SortApplicationsPipe } from './sort-applications.pipe';

describe('SortApplicationsPipe', () => {
  it('create an instance', () => {
    const pipe = new SortApplicationsPipe();
    expect(pipe).toBeTruthy();
  });
});

import { TopWeaponAltPipe } from './top-weapon-alt.pipe';

describe('TopWeaponAltPipe', () => {
  it('create an instance', () => {
    const pipe = new TopWeaponAltPipe();
    expect(pipe).toBeTruthy();
  });
});

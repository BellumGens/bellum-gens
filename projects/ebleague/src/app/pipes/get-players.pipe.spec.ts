import { GetPlayersPipe } from './get-players.pipe';

describe('GetPlayersPipe', () => {
  it('create an instance', () => {
    const pipe = new GetPlayersPipe();
    expect(pipe).toBeTruthy();
  });
});

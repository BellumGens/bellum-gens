import { NotInGroupPipe } from './not-in-group.pipe';

describe('NotInGroupPipe', () => {
  it('create an instance', () => {
    const pipe = new NotInGroupPipe();
    expect(pipe).toBeTruthy();
  });
});

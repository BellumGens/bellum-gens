import { GetRegCountPipe } from './get-reg-count.pipe';

describe('GetRegCountPipe', () => {
  it('create an instance', () => {
    const pipe = new GetRegCountPipe();
    expect(pipe).toBeTruthy();
  });
});

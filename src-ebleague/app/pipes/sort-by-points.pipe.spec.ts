import { SortByPointsPipe } from './sort-by-points.pipe';

describe('SortByPointsPipe', () => {
  it('create an instance', () => {
    const pipe = new SortByPointsPipe();
    expect(pipe).toBeTruthy();
  });
});

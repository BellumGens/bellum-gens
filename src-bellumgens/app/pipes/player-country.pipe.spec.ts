import { PlayerCountryPipe } from './player-country.pipe';

describe('PlayerCountryPipe', () => {
  it('create an instance', () => {
    const pipe = new PlayerCountryPipe();
    expect(pipe).toBeTruthy();
  });
});

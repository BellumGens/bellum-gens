import { NotificationStatePipe } from './notification-state.pipe';

describe('NotificationStatePipe', () => {
  it('create an instance', () => {
    const pipe = new NotificationStatePipe();
    expect(pipe).toBeTruthy();
  });
});

import { UnreadNotificationsPipe } from './unread-notifications.pipe';

describe('UnreadNotificationsPipe', () => {
  it('create an instance', () => {
    const pipe = new UnreadNotificationsPipe();
    expect(pipe).toBeTruthy();
  });
});

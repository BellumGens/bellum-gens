import { OpenPositionsPipe } from './open-positions.pipe';

describe('OpenPositionsPipe', () => {
  it('create an instance', () => {
    const pipe = new OpenPositionsPipe();
    expect(pipe).toBeTruthy();
  });
});

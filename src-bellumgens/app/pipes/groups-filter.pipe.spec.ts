import { GroupsFilterPipe } from './groups-filter.pipe';

describe('GroupsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new GroupsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
